## Summary
Create a dedicated public **How it works** page linked from the landing header.

## Why
The landing header exposes a "How it works" entry that should resolve to a full explanatory page with clear product flow and onboarding guidance.

## Scope (Frontend only)
- Add route: `/how-it-works`
- Add page component under `apps/frontend/src/pages/**`
- Update landing header link "How it works" to navigate to `/how-it-works`
- Implement step-by-step sections for:
  1. Create account / Sign in
  2. Create event and add people
  3. Add entries and split modes
  4. Review balances and settle
- Include friendly, direct explanatory copy consistent with UX guidelines.
- Ensure mobile-first responsive behavior with MUI components.

## Out of Scope
- Backend changes
- Contracts/API changes
- New domain/business rules

## Acceptance Criteria
- [ ] `/how-it-works` route is implemented and reachable from landing header.
- [ ] Page explains the end-to-end flow with clear step hierarchy.
- [ ] Uses MUI components and matches `/docs/frontend/ux-guidelines-v1.md`.
- [ ] No placeholder-only sections or broken navigation.
- [ ] CTA hierarchy remains auth-first (`Sign in` primary, `Create account` secondary).
- [ ] FE checks pass:
  - `npm run lint`
  - `npm run test:run`
  - `npm run build`

## Notes
- This issue must remain **frontend-only**.
- Do not introduce API/contract requests from this issue.
